export default {
  async test() {
    return true;
  },

  async single(query, options) {
    return [];
  },

  async batch(query, options) {
    return [];
  },

  async movie(query, options) {
    return [];
  }
};
